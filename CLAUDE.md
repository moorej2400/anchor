# CLAUDE.md — Anchor

> ## ⚠️ THIS IS A PUBLIC REPOSITORY — NEVER COMMIT PERSONAL INFORMATION
>
> No real API keys/tokens/secrets, no real usernames or emails, no
> machine-specific absolute paths, no contents of `~/.anchor/`, `~/.claude/`,
> `~/.codex/`, `~/.copilot/`, or opencode data. Fixtures are synthetic only.

Read [`AGENTS.md`](AGENTS.md) and then [`docs/SPEC.md`](docs/SPEC.md) (single
source of truth) before doing anything. The UI must be built to the mock at
[`docs/Anchor.dc.html`](docs/Anchor.dc.html).
