# CLI Manager (Anchor) — Design Spec (superseded)

This early design document has been superseded by the full, standalone
specification at [`docs/SPEC.md`](../../SPEC.md), which incorporates the
authoritative UI mock ([`docs/Anchor.dc.html`](../../Anchor.dc.html)) and the
three-phase implementation plan. **Read `docs/SPEC.md` — do not implement from
this file.**
